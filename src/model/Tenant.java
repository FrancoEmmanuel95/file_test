package model;

public class Tenant extends Clients {
    public Tenant(Integer id, String name, String contactNumber, String email, String address) {
        super(id, name, contactNumber, email, address);
    }
}
